model Guild {
    id String @id

    members        Member[]
    levelRewards   GuildLevelReward[]
    ignoredChannel GuildIgnoredChannel[]
    shopItems      ShopItem[]
    settings       GuildSettings[]

    blacklists BlacklistGuild[]

    createdAt DateTime @default(now())
}