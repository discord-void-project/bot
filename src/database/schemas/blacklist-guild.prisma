model BlacklistGuild {
    userId String @id
    user   Blacklist @relation(fields: [userId], references: [userId], onDelete: Cascade)

    guildId String
    guild   Guild  @relation(fields: [guildId], references: [id], onDelete: Cascade)

    accepted Boolean?

    @@unique([guildId, userId])
    @@map("blacklist_guild")
}
