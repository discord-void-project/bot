model GuildCurrency {
    guildId String
    guild   Guild  @relation(fields: [guildId], references: [id], onDelete: Cascade)

    name        String
    description String?
    iconUrl     String?

    shops Shop[]

    @@id([guildId, name])
    @@map("guild_currency")
}
