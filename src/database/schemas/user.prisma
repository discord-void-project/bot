model User {
    id    String @id
    flags Int    @default(0)

    guilds      Member[]
    dailyQuests MemberDailyQuest[]

    blacklister Blacklist[] @relation("Mod")
    blacklisted Blacklist[] @relation("User")

    tagAssignedAt DateTime?
    createdAt     DateTime  @default(now())
}
